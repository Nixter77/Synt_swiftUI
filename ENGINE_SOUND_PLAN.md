# План: звук уровня Surge / Vital (не список фич)

Цель — чтобы аккорд из 4 нот, лид и пэд звучали как у аккуратного вычитающего синта, а не как у учебного движка с заплатками.

Serum и Vital чистые не потому, что они VST. У них **голос = маленький синтезатор**, статичный гейн, антиалиас источника, фильтр на каждый голос, FX как микшер. Открытый эталон — Surge XT. Мы копируем **эти законы**, не интерфейс и не палитру.

Стать Serum за месяц нельзя: у них годы C++/SIMD и библиотека столов. Стать синта с той же дисциплиной тракта — можно, и это как раз то, чего не хватает.

---

## Почему «патчить тут» не работает

`SOUND_QUALITY_NEXT.md` — полировка поверх учебной схемы. Мы так и жили: Hermite, 1/N, отключить лимитер, починить delay, обрезать mip.

Это лечит симптомы. Архитектура та же:

```
все голоса → одна сумма → 1/N → один biquad на шину → клип → Apple Delay/Reverb
```

У Surge / Vital / Diva:

```
голос: osc + свой фильтр + свой ADSR
     ↓
линейная сумма (без 1/N на шине)
     ↓
небольшой потолок
     ↓
FX с запасом по уровню
```

Пока схема первая — четвёртая нота будет то хрустеть, то проваливаться, то зависать в хвосте FX. Полировка это не закроет.

Старые файлы (`FUTURE_ROADMAP.md`, granular / FM / MPE / AI) — **палитра**. Их морозим, пока тракт не звучит.

---

## Пять законов, которые мы берём

1. **Голос = инструмент.** Свои фазы осцилляторов, свой фильтр, свой ADSR. Сумма — после этого.
2. **Гейн статичный.** Один голос тихий (−12…−6 dBFS). Четыре ноты **громче одной** (так и должно быть). Запас и мягкий потолок, не деление всей шины на N.
3. **Источник бандлимичен.** Пила/квадрат — BLEP. Wavetable — mip по Найквисту на верхней частоте таблицы + Hermite. Нелинейности (клип, drive) — оверсэмпл 2×, не весь синта.
4. **Фильтр на голос, модуляция в октавах.** Один biquad на аккорд — интермодуляция и «хруст на 4-й». Env → cutoff только по отдельному «да».
5. **FX как AUX, не как помойка.** Delay/reverb после синтеза, feedback < 1, клип **после** мокрых или достаточно тихий сенд. Сейчас Apple FX сидят за нашим клиппером — любой плотный аккорд там может хрустеть, тесты этого не видят.

Не законы и не в этом плане: третья волна осциллятора, warp как в Serum, granular, FM, MPE, облако пресетов, переписывание на JUCE «чтобы стало VST».

---

## Где мы сейчас

Уже близко к синта и должно остаться:

- пул 32 голоса, очередь noteOn/noteOff без лока;
- у голоса свои фазы и ADSR;
- PolyBLEP 2-point / PolyBLAMP, wavetable Hermite;
- mip режется по Найквисту (после хруста Crystal Lead);
- delay feedback 0…1, не 100%;
- зависания noteOff (keyCode, зона клавиатуры) закрыты;
- advanced FX по умолчанию выключены.

Дыры, из-за которых мы не «как Surge»:

| Закон | Сейчас | Что слышно |
|---|---|---|
| Голос | фильтр на каждый голос (1.2 принято) | кастрюльный хруст спал; 4-я нота ещё зрипит |
| Гейн | статичный, линейная сумма (1.1 принято) | 4 ноты громче 1 |
| Источник | 2-point BLEP; рендер по 1 сэмплу на Swift | риск хруста от CPU на плотных WT |
| Фильтр | RBJ 12 дБ, LFO в герцах | тонкий резонанс, не музыкальный свип |
| FX | Delay→Reverb после клиппера | мокрый аккорд хрустит «после» синтеза |

**Не писать движок с нуля.** Схема уже та же семья, что у Surge/Vital (голос → сумма → потолок → FX). С нуля = ещё много часов, чтобы Init снова заиграл, плюс риск фазы A. 6 часов ушли на пожары фазы 0 и на 1.1/1.2, не на «неправильный тип синта».

**Фабрика переписана под живой тракт** (не под будущий движок): Env Amt = 0, матрица только pitch/amp/pan, один wavetable, unison только на бас/лид, реверб ≤ 0.24, без свалок FX. Init не трогали.

Init и фабрика — священны. Любой шаг, который меняет тело Init, только с явным слухом и правом отката.

---

## Фазы

Одна фаза → пересборка → слух. Нет «ок» — не идём дальше.  
Слушать всегда: **1 нота / 3 ноты / 4 ноты / Init / Crystal Lead / Slow Motion**.

Модуляцию фильтра (Env Amt / matrix → cutoff) без отдельного «да» не включать.

### Фаза 0 — стоп пожарам (сделано, не переоткрывать)

NoteOff, delay 100%, синяя зона клавиатуры, Nyquist mip, лимитер выключен.  
Если снова «зависло / само звучит» — это регресс фазы 0, не повод прыгать в фазу 3.

### Фаза 1 — стать синта по схеме (главное)

Это единственный прыжок, который реально приближает к Surge. Полировка без неё бесполезна.

**1.1 Статичный гейн, убрать шинный `1/N`** — принято (лучше; 4-я нота ещё зрипит)

- У голоса фиксированный `AudioMath.voiceGain` (сейчас `1` = одна нота Init как раньше).
- Сумма линейная. 4 ноты громче 1, но под потолком.
- Мягкий клип только как сетка, не как характер.
- Unison считает `1/U` **внутри ноты**, не по всей шине.

Риск: одна нота тише или аккорд снова горячий. Крутить только коэффициент, не возвращать `1/N`.

Слух: Init 1 vs 4 — аккорд мощнее, без хруста. Crystal Lead 4 — без песка.

**1.2 Фильтр на каждый голос (тот же biquad)** — принято (получше; 4-я ещё зрипит; WT Orchestra отдельно плохо)

- Состояние фильтра живёт в голосе. Шинный `filterL/R` убрать из суммы осцилляторов.
- Тип/формула те же. Init на одной ноте почти не меняется.
- Аккорд = 4 независимых фильтра, как у Vital/Surge.

Риск: чуть другой резонанс на аккорде (это как раз цель). CPU выше — рендер уже не по одному сэмплу, см. 2.1 если не влезем.

Слух: 4 ноты перестают «хрустеть в одной кастрюле». Одна нота как была.

**1.3 Не трогать в этой фазе**

Env Amt, новый SVF, оверсэмпл, свои delay/reverb, VoiceManager.

### Фаза 2 — источник и CPU как у взрослых

Только после «4 ноты чистые» на фазе 1.

**2.1 Блочный рендер (8–32 сэмпла)**  
Не слышно, если правильно. Нужно, чтобы 6–8 голосов не хрустели от недобега callback. Внутренности без смены тембра.

**2.2 4-point PolyBLEP** на пиле/квадрате. Init чуть глаже на C6+. Съело яркость — откат.

**2.3 Частота устройства**, не жёсткие 44.1. Иначе Apple сама ресэмплит в 48 кГц и гадит.

Mip/Hermite уже сделаны — не открывать заново.

### Фаза 3 — фильтр «как Diva», opt-in

**3.1 TPT/SVF 12 дБ рядом с biquad, выключен по умолчанию.** Init остаётся на biquad.  
**3.2** Env → cutoff — только после отдельного «да», и в октавах, не `+N гц`.

### Фаза 4 — FX как микшер

**4.1** DC HPF ~12–18 Гц на сумме, до FX. Суб на месте.  
**4.2** Потолок **после** Apple Delay/Reverb (или свой delay с честным feedback и damp).  
**4.3** Chorus: Hermite чтения линии. Init mix=0 — тишина.  
**4.4** 2× оверсэмпл только distortion, когда он включён.

### Фаза 5 — не звук, а доставка

AUv3 / Logic — **после** того, как standalone уже слушается. Формат плагина звук не улучшит.

---

## Что сознательно не делаем

- Переписывать всё на JUCE, «чтобы стать VST».
- Granular, FM, MPE, AI-пресеты, импорт serum-столов — пока тракт сырой.
- Глобальный оверсэмпл 4× всего синта.
- Лестница Moog и analog drift как дефолт (характер, не чистота).
- Вернуть `VoiceManager` в render.
- Включить Env Amt «за компанию» с per-voice фильтром.

---

## Порядок нельзя ломать

```
1.1 гейн  →  1.2 фильтр на голос  →  2.x источник/блоки  →  3 фильтр-модель  →  4 FX
```

BLEP или SVF до смены схемы = снова полировка кастрюли.  
SVF до статичного гейна = красивый фильтр, который клипит на аккорде.

---

## Как принимаем фазу

1. Один подпункт (1.1, потом 1.2, …).
2. Тест: нет NaN, Init 4 ноты < потолка, Crystal Lead отпускается.
3. Ты слушаешь список выше.
4. «ок» → дальше. «нет» → откат этого подпункта, стоп.

Старт работы — только после «да» на **1.1**. Пока да нет, код не трогаем.
